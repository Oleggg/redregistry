.row{:class => "fields"}
  .col.center
    = f.select :doctor_id, Doctor.for_select
  .col
    = f.text_field :registration,:class => "datepick",:size => 9
  .col.center
    = f.text_field :comment, :size => "50"
    = link_to_remove_fields "Удалить", f

:javascript 
  $(document).ready(function() {
      $(".datepick").datepicker({ buttonText: 'choose' });
    });

