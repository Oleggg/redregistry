= semantic_form_for [:vocabularies, @child_help] do |f|
  = f.inputs do
    = f.input :name, :required => true
  = f.buttons do
    = f.commit_button "Сохранить"
    %li{:class=>"link"}
      или
      = link_to 'Вернутся', vocabularies_child_helps_path

