= semantic_form_for [:vocabularies, @health_status] do |f|
  = f.inputs do
    = f.input :name, :required => true
  = f.buttons do
    = f.commit_button "Сохранить"
    %li{:class=>"link"}
      или
      = link_to 'Вернуться', vocabularies_health_statuses_path

