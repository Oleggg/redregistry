= semantic_form_for [:vocabularies, @department] do |f|
  = f.inputs do
    = f.input :name, :required => true
    = f.input :full_name
    = f.input :phone1
    = f.input :phone2
    = f.input :comment

  = f.buttons do
    = f.commit_button "Сохранить"
    %li{:class=>"link"}
      или
      = link_to 'Вернутся', vocabularies_family_helps_path

