= semantic_form_for [:vocabularies, @profession] do |f|
  = f.inputs do
    = f.input :name, :required => true
  = f.buttons do
    = f.commit_button "Сохранить"
    %li{:class=>"link"}
      или
      = link_to 'Вернутся', vocabularies_professions_path

