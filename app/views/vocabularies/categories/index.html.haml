- title(Category.model_name.human)

%h1= Category.model_name.human

%table
  %tr
    %th= Category.human_attribute_name(:id)
    %th= Category.human_attribute_name(:name)
    %th{:colspan=>"2"}

  - @categories.each do |category|
    %tr
      %td= category.id
      %td= category.name
      %td= link_to 'Изменить', edit_vocabularies_category_path(category)
      %td= link_to 'Удалить', vocabularies_category_path(category), :confirm => 'Вы уверены?', :method => :delete

= will_paginate @categories

= link_to 'Добавить', new_vocabularies_category_path

