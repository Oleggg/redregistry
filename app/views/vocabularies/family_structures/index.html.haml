- title(FamilyStructure.model_name.human)

%h1= FamilyStructure.model_name.human

%table
  %tr
    %th= FamilyStructure.human_attribute_name(:id)
    %th= FamilyStructure.human_attribute_name(:name)
    %th{:colspan=>"2"}

  - @family_structures.each do |family_structure|
    %tr
      %td= family_structure.id
      %td= family_structure.name
      %td= link_to 'Изменить', edit_vocabularies_family_structure_path(family_structure)
      %td= link_to 'Удалить', vocabularies_family_structure_path(family_structure), :confirm => 'Вы уверены?', :method => :delete

= will_paginate @family_structures

= link_to 'Добавить', new_vocabularies_family_structure_path

