= semantic_form_for [:vocabularies, @cc_article] do |f|
  = f.inputs do
    = f.input :number, :required => true
    = f.input :name, :required => true
  = f.buttons do
    = f.commit_button "Сохранить"
    %li{:class=>"link"}
      или
      = link_to 'Вернуться', vocabularies_cc_articles_path

